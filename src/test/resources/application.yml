server:
  port: 8080  # 服务器端口号设置
  servlet:
    context-path: /vue
    jsp:
      init-parameters:
        development: true

mybatis-plus:
  mapper-locations: classpath:mapper/*Mapper.java


spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/vue?serverTimezone=UTC
    username: root
    password: 123456